# Docs for the Azure Web Apps Deploy action: https://github.com/Azure/webapps-deploy
# More GitHub Actions for Azure: https://github.com/Azure/actions

name: Build and deploy container app to Azure Web App - Thamco-product

on:
  push:
    branches:
      - Mains
  workflow_dispatch:

jobs:
  build:
    runs-on: 'ubuntu-latest'

    steps:
    - uses: actions/checkout@v2

    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v2

    - name: Log in to registry
      uses: docker/login-action@v2
      with:
        registry: https://thamcregistry.azurecr.io/
        username: ${{ secrets.AzureAppService_ContainerUsername_919f4db6585946cb9276e8bab65d8fdf }}
        password: ${{ secrets.AzureAppService_ContainerPassword_b7ae2ae5ead64d26b8d860adda04402a }}

    - name: Build and push container image to registry
      uses: docker/build-push-action@v3
      with:
        push: true
        tags: thamcregistry.azurecr.io/${{ secrets.AzureAppService_ContainerUsername_919f4db6585946cb9276e8bab65d8fdf }}/productservice:${{ github.sha }}
        file: ./Dockerfile

  deploy:
    runs-on: ubuntu-latest
    needs: build
    environment:
      name: 'production'
      url: ${{ steps.deploy-to-webapp.outputs.webapp-url }}

    steps:
    - name: Deploy to Azure Web App
      id: deploy-to-webapp
      uses: azure/webapps-deploy@v2
      with:
        app-name: 'Thamco-product'
        slot-name: 'production'
        publish-profile: ${{ secrets.AzureAppService_PublishProfile_300f943a95dc4ccdb4e632a0f5a70778 }}
        images: 'thamcregistry.azurecr.io/${{ secrets.AzureAppService_ContainerUsername_919f4db6585946cb9276e8bab65d8fdf }}/productservice:${{ github.sha }}'